# Single RG, Vnet and VM

It deploys a single VM on Azure with a public IP. Just it.

Using default values:
- Use your current username as VM admin account.
- Try to use any public key available in your  `~/.ssh` directory for the admin account.
- Maximize the cost saving with cheaper VM profile decisions.
